# ============================================================
# Dependencias para EJECUTAR LOS TESTS en local (entorno .venv)
# ============================================================
# Instalación:
#   source .venv/bin/activate
#   pip install -r requirements-test.txt
#
# Nota: el resto de librerías de tiempo de ejecución de los
# microservicios (fastapi, httpx, pydantic, pymongo, bcrypt,
# qdrant-client, requests, uvicorn) ya deben estar instaladas en
# el .venv. Si partes de un entorno limpio, instala también los
# requirements.txt de cada servicio. Las dependencias de Google
# (Vertex AI) NO son necesarias: los tests las simulan.

# Framework de test y cobertura
pytest>=8.0,<9.0
pytest-cov>=5.0

# Necesarias para importar los servicios bajo test y que faltan
# habitualmente en el .venv:
PyJWT==2.12.1          # 'import jwt' en auth, gateway e history
email-validator>=2.0   # 'EmailStr' de pydantic en auth y gateway
